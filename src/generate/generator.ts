import Database from "@dbml/core/types/model_structure/database";
import DomainFormat from "src/format/data/domain";

export default class Generator {
    public static generateDbml(model: Database, domain: DomainFormat) {
        
    }
}